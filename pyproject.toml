[tool.poetry]
name = "redis-cluster"
version = "0.1.0"
description = ""
authors = ["lcorrea <lcorrea@florestleaks.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
